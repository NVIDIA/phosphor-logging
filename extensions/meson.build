if get_option('openpower-pel-extension').allowed()
    subdir('openpower-pels')
endif

if not get_option('nvbmc-logging-extension').disabled()
    subdir('nvbmc-logging')
endif

#subdir('nvbmc-logging')
