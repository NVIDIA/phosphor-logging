# gcovr configuration file
# Filters for files authored by nvidia.com contributors
# Generated automatically by generate-nvidia-gcovr.sh

# Exclude compiler-generated exception-handling branches (e.g. copy/move      
# constructors of std containers) that are not reachable in normal code paths.
exclude-throw-branches = yes                                                  

# Exclude branches in lines that are not reachable (compiler dead code).      
exclude-unreachable-branches = yes 

filter = bin\.hpp
filter = config_main\.h
filter = elog_entry\.cpp
filter = elog_entry\.hpp
filter = elog_serialize\.cpp
filter = elog_serialize\.hpp
filter = extensions\.cpp
filter = extensions\.hpp
filter = logging_test\.cpp
filter = log_manager\.cpp
filter = log_manager\.hpp
filter = log_streamer\.cpp
filter = log_streamer\.hpp
filter = util\.cpp
filter = util\.hpp

html = yes
print-summary = yes
decisions = yes